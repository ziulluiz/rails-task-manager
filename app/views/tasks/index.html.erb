<div>
  <h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
  <br>
  <hr>
  <button><%= link_to "New task", tasks_new_path %></button>
</div>
